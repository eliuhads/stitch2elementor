---
description: MCP (Model Context Protocol) server builder. Creates custom MCP tools and servers to connect AI agents with external APIs, databases and services.
triggers: mcp, model context protocol, mcp server, mcp tool, claude tools, cursor mcp
---

# MCP Builder
MCP (Model Context Protocol) server builder. Creates custom MCP tools and servers to connect AI agents with external APIs, databases and services.
